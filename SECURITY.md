# 安全说明

本项目用于公司内部项目管理，涉及业务数据、账号会话与 AI 服务凭据（API Key、JWT 等）。

## 报告安全漏洞

请**不要**在公开 Issue 中披露安全漏洞。请通过以下方式私下报告：

- GitHub Private Vulnerability Reporting（推荐，启用后可在仓库 Security 页提交）
- 或直接联系维护者（见 git 提交记录中的作者信息）

## 提交内容要求

1. 描述漏洞类型、影响范围与复现步骤（可包含最小 PoC）。
2. 说明受影响的版本/分支。
3. 给出可能的修复建议（可选）。

我们会在确认后尽快跟进，并在修复发布前不对公开仓库的默认分支推送带漏洞的版本。

## 已知安全边界

- 生产模式强制要求 `JWT_SECRET` 与 `AI_CONFIG_ENCRYPTION_KEY` 独立且长度 ≥ 16。
- API Key 仅在后端加密存储，前端不回显明文。
- 默认使用 SQLite 单机部署；如接入 PostgreSQL，请确保网络隔离与最小权限账号。
