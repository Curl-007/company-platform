# 架构设计评估与升级建议

> 评估对象：公司/AI 项目管理平台（npm workspaces monorepo：`api` + `web`）  
> 评估性质：架构设计质量评估 + 演进路线建议（非 PR 级代码审查全文）  
> 评估日期：2026-07-13  
> 代码基线说明：仓库基线分支为 `master`（无 `main`）；大量模块化/容量/状态机等升级位于工作区未提交变更，本文以**工作区实际代码**为准。

---

## 1. 总评

| 项 | 判断 |
|----|------|
| **档位** | **B（中上）** — 试点/内部交付平台可用，尚未到“生产治理级” |
| **综合分** | **约 3.1 / 5** |
| **一句话** | 方向正确：模块化单体 + 领域垂直切片 + 状态机/乐观锁/部分 Policy；但入口与数据层仍偏原型，契约与前端可维护性滞后于业务宽度。 |

**经验判断**：比“大单文件原型”成熟一档，比“可长期多人协作的领域平台”还差一档。最大价值不在立刻换 Nest/PostgreSQL，而在**把已经做对的边界做完整、把上帝文件和双真源收口**。

### 产品定位（架构硬约束）

- 服务目标：**项目交付 + 团队容量 + 风险管理**
- **非目标**：自动绩效、排名、以容量模块伪装绩效考核

---

## 2. 当前架构画像

### 2.1 技术栈（现状）

| 层 | 技术 |
|----|------|
| Monorepo | npm workspaces（`api` + `web`） |
| 后端 | Express 4、JavaScript（CommonJS）、JWT、WebSocket（`ws`） |
| 数据 | SQLite（`node:sqlite`）、版本化 `migrations/` |
| 契约 | OpenAPI 3（部分路径） |
| 前端 | React 19、TypeScript、Vite 6、HashRouter |
| 质量 | API：`node --test`；Web：以 typecheck 为主；GitHub Actions CI |

### 2.2 目标结构（进行中）

后端目标形（摘自 `docs/architecture-refactor-plan.md`）：

```text
api/
  server.js                 # 兼容入口 / 组合根
  src/
    security/
    middleware/
    workflow/
    modules/
      <domain>/
        routes.js
        service.js
        repository.js
```

前端目标形：

```text
web/src/
  services/
    api.ts / apiClient.ts / resources.ts（临时 barrel）
  features/
    <domain>/
      api.ts
      components/
```

### 2.3 热点规模（评估时快照）

| 路径 | 约行数 / 规模 | 含义 |
|------|----------------|------|
| `api/server.js` | ~4311 | 组合根 + 仍有残留路由/逻辑 |
| `api/db.js` | ~1300 | 连接、迁移、映射、通用写原语混合 |
| `web/src/pages/ProjectsPage.tsx` | ~1791 | 前端巨石页 |
| `api/openapi.json` | ~26 paths | 远小于运行时路由面 |
| `api/src/modules/*` | 十余个业务域 | 垂直切片已铺开 |

---

## 3. 当前架构的优点

1. **模块化单体选型合理**  
   交付/容量/风险一体产品，用 monorepo + 垂直模块，比过早微服务更匹配团队与流量。

2. **后端分层目标清晰且部分落地**  
   `routes → service → repository`，横切 `security` / `workflow`，符合可演进的 Express 模块化路径；重构执行顺序正确（先基建、再按域拆、保 URL/响应形状）。

3. **领域规则开始显式化**  
   `api/src/workflow/stateMachine.js` 用冻结表表达状态与合法迁移，比散落条件判断更可测试、可审计；配合 status history、sprint commitment、交付 SoD 等，说明在补“流程平台”内核。

4. **并发与写路径意识在进步**  
   乐观锁 `version`、幂等键、相关集成测试（冲突/门禁）是从 demo 走向可信写入的正确信号。

5. **权限模型有中心化意图**  
   `accessControl` + `projectAccess` + capabilities，优于“每个接口手写角色字符串”；前后端有对齐意识。

6. **前端过渡策略务实**  
   `apiClient` + `features/*/api` + `resources` 兼容 barrel，可在不重写全站的情况下渐进迁移。

7. **质量门禁开始存在**  
   API 测试、（子集）契约测试、CI typecheck/build，使结构搬家具备最低护栏。

---

## 4. 架构问题与风险

### 4.1 结构债

| 问题 | 影响 |
|------|------|
| **双入口心智** | `server.js` 与 `modules` 双轨，新人分不清逻辑归属 |
| **数据层上帝对象** | `db.js` 职责过重，原语层安全/并发策略不统一 |
| **前端巨石页** | 大页吞业务，features 目录价值未吃满 |
| **兼容层寿命不清** | `resources.ts` 若不设删除条件，将永久双 API 面 |

### 4.2 领域边界

- 跨域规则（如发布门禁依赖需求就绪、任务闭环、缺陷）易在多处重复实现。
- 状态机覆盖 project / requirement / task / sprint；build / release / defect 等未完全统一进同一工作流内核。
- 容量模块若指标语义膨胀，可能滑向“自动绩效”——需在架构上明确 **non-goal 守卫**。

### 4.3 数据架构

- SQLite 文件库适合单机试点，不适合作为多写、强审计、备份恢复的终局假设。
- 缺少统一外键 / 软删 / 聚合删除策略时，不变量依赖手写 SQL，脆弱。
- 危险写原语（如不当的 `INSERT OR REPLACE`、`MAX(id)+1` 发号）属于**数据层设计债**，不是局部小 bug。
- 已有版本化 migration，方向正确，但与“逻辑关联 + 兼容 ALTER”并存会累积演进成本。

### 4.4 API 契约

- OpenAPI 覆盖远小于运行时路由 → **没有可靠的单一真源**。
- 前后端类型、枚举、角色能力双维护，漂移成本高。
- 缺少清晰的 API 版本策略（如 `/api/v1`）时，契约与实现只会继续分叉。

### 4.5 前端架构

- HashRouter + 自管 hash query，距离可嵌套路由、深链、清晰代码分割仍有差距。
- 缺少统一的服务端状态/缓存失效策略时，大页本地 state 会继续膨胀。
- 前端几乎只有 typecheck → 结构拆分缺少行为护栏。

### 4.6 安全与运维（架构层）

- 鉴权/密钥对运行环境与弱默认敏感 → 部署模型未成为架构一等公民。
- WebSocket token 传输方式、过大 JSON body limit 等说明**边界网关假设缺失**。
- 配置、健康检查、备份、trust proxy 等运维面偏弱。

### 4.7 演进路径冲突（战略债）

两套文档在抢“成功标准”：

| | `docs/architecture-refactor-plan.md` | `项目管理平台统一优化设计方案.md` |
|--|--------------------------------------|-------------------------------------|
| 目标 | 保行为拆分、feature 模块化 | 生产化能力 + 目标技术栈（Nest/PG 等） |
| 风险 | 拆完仍是弱数据层/弱契约 | 边界未稳就换栈，迁移成本爆炸 |

**结论**：当前最大战略风险不是“技术旧”，而是**同时按两套成功标准验收**。

---

## 5. 与常见 PM/交付平台架构对比

| 能力 | 常见成熟形态 | 本项目现状 |
|------|--------------|------------|
| 应用形态 | 模块化单体或少数服务 | 模块化单体（进行中） |
| 写模型 | 聚合 + 不变量 + 状态机 | 部分状态机；聚合边界偏弱 |
| 契约 | OpenAPI/Proto 真源 + 生成客户端 | 手写双端；OpenAPI 子集 |
| 权限 | 统一 Policy（角色 + 资源范围） | 中心化意图有，全资源未闭环 |
| 数据 | PostgreSQL + FK + 迁移 + 备份 | SQLite 试点级 |
| 前端 | 域 feature + 薄 page + Query | feature API 有，巨石 page 仍在 |
| 集成 / AI | 防腐层 + 密钥 / 配额 / 审计 | AI 仍偏挂在大入口 |

**对标结论**：业务宽度已接近“小型交付平台 + 容量”，工程内核仍处于**原型加固阶段**。关键是不要用换栈掩盖边界债。

---

## 6. 评分卡（1–5）

| 维度 | 现状分 | 说明 |
|------|--------|------|
| 产品边界一致性 | 4 | 定位清晰；需防容量滑向绩效 |
| 分层与职责 | 3 | 目标对，入口/`db` 未收完 |
| 模块边界 | 3.5 | 垂直切片好；跨域编排弱 |
| 领域 / 状态 | 3.5 | 核心域有状态机；未全域统一 |
| API 契约 | 2 | 双端手写 + 覆盖不足 |
| 数据架构 | 2.5 | 迁移有了；约束/并发模型弱 |
| 安全与数据范围 | 3 | 有亮点；未形成统一 Policy 面 |
| 前端架构 | 2.5 | feature API 起步；巨石页拖后腿 |
| 可测试性 | 3 | 集成强、模块单测/前端弱 |
| 可运维性 | 2.5 | CI 有；部署/密钥/备份弱 |
| 演进路径清晰度 | 3 | 有计划；双文档冲突扣分 |
| **综合** | **~3.1（B）** | 值得继续投入；先收口再换栈 |

---

## 7. 值得升级的部分（按 ROI）

### 7.1 近期（1–2 周，低风险高收益）— 强烈建议

| 升级项 | 为什么 | 怎么做 | 预期收益 | 风险 |
|--------|--------|--------|----------|------|
| 收口组合根 | `server.js` 双轨拖垮可理解性 | AI/dashboard 等下沉 module；server 只装配 | 可维护性 ↑ | 低（保 URL） |
| 数据访问消毒 | 写原语不安全/不并发友好 | 表白名单、收敛危险 REPLACE、改进 ID 策略 | 正确性/安全 ↑ | 中 |
| Policy 闭环 | 范围过滤不能只在 projects | 统一 project scope 应用到需求/任务/缺陷等列表 | 安全可信 ↑ | 中 |
| 契约分层声明 | 26 path 装成全量会误导 | 扩 OpenAPI，或明文“核心写路径子集”+ CI 校验该子集 | 协作成本 ↓ | 低 |
| 前端去巨石 | Projects 等大页是生产力黑洞 | 只拆 UI section 到 features，不改行为 | 并行开发 ↑ | 低 |
| 安全启动契约 | JWT/加密密钥是架构边界 | 启动强校验、禁止弱默认密钥 | 生产底线 | 低 |

### 7.2 中期（1–2 月）— 性价比最高的“平台化”

| 升级项 | 为什么 | 怎么做 | 预期收益 |
|--------|--------|--------|----------|
| 统一工作流内核 | 门禁/状态散落会复制 | build/release/defect 并入统一 transition + history | 规则一处修改 |
| 跨域 Application Service | 发布门禁跨多表 | 如 `DeliveryGateService` 编排，repo 不互叫业务 | 边界清晰 |
| 错误模型与写路径标准化 | 可信写入需要一致语义 | 统一 error code、按路由 body limit、AI 独立限流 | 运维/安全 |
| 前端状态与缓存策略 | 粗暴清缓存不可扩展 | 引入 TanStack Query（或等价）按域失效 | UX 与复杂度可控 |
| 测试金字塔校正 | 超大集成测试难定位 | service 纯逻辑单测 + 少量契约/集成 | 重构速度 |
| 数据约束增强（仍可 SQLite） | 不必先换 PG | FK/唯一约束、软删约定、备份脚本、关键索引 | 数据可信度 |

### 7.3 远期（平台级）— 有门槛再上

| 升级项 | 建议触发条件 | 说明 |
|--------|--------------|------|
| PostgreSQL | 多实例写、备份/审计合规、并发冲突成为真实痛点 | 解决运维与并发模型，不是目录结构问题 |
| Nest / Fastify 外壳 | 模块边界已稳定、DI/模块图清晰 | 否则只是把浆糊搬进框架 |
| Orval 等客户端生成 | OpenAPI 先成为真源 | 生成前必须先扩契约 |
| 对象存储 / 队列 | 文件与 AI Job 量上来 | 现可先做防腐层，本地存储可过渡 |
| Path Router 信息架构 | 导航 IA 产品稳定 | 与 Hash 壳迁移一并规划 |

---

## 8. 不建议现在做（避免过度工程）

1. **立刻上微服务 / 多仓** — 域耦合高，团队与流量不支撑。  
2. **未稳边界就全面 Nest + Prisma + PostgreSQL + Redis + BullMQ + MinIO** — 目标态可以保留，但不适合作为“下一步”。  
3. **为 AI 单独拆复杂平台** — 先做好密钥、配额、审计、上下文范围的防腐层。  
4. **全局重型状态管理（为现代化而 Redux 化）** — 优先域 feature + Query。  
5. **自动绩效 / 排名能力** — 与产品边界冲突，架构上应明确拒绝。  
6. **为了“看起来现代”重写全部页面** — 继续保行为切片收益更高。

---

## 9. 推荐演进路径（路线 C：混合渐进）

三选一策略对比：

| 路线 | 内容 | 适用 |
|------|------|------|
| **A. 保行为深化模块化** | 继续 Express + SQLite，收口分层/大页/契约 | 只求可维护、短期交付 |
| **B. 生产化换栈** | 按统一方案直接 Nest + PG 等 | 已有强运维/合规压力且边界稳定 |
| **C. 混合渐进（推荐）** | 用结构计划纪律推进模块化；用统一方案阶段 0–2 思想补强可信度；换栈设门槛 | **默认推荐** |

### 阶段图

```text
Phase 0  真源与止血（1–2 周）
  ├─ 选定文档真源策略（结构计划负责执行；统一方案作北极星）
  ├─ 安全启动 / 写路径危险原语 / WS 与 body 边界
  └─ OpenAPI 范围声明 + CI 门禁对齐

Phase 1  边界完整（2–6 周）
  ├─ server.js 瘦身为 composition root
  ├─ Policy / 项目范围全列表闭环
  ├─ 工作流内核扩展 + DeliveryGate 去重
  └─ 前端巨石页按域拆完；resources 设定删除里程碑

Phase 2  数据可信（并行或紧随）
  ├─ 约束、软删约定、备份、关键索引
  ├─ 幂等 / 乐观锁覆盖关键写路径
  └─ service 单测补齐

Phase 3  体验与契约工业化
  ├─ OpenAPI 扩到主路径 + 客户端对齐/生成
  ├─ Query 缓存与路由 IA
  └─ 观测性（结构化日志、审计查询）

Phase 4  基础设施换栈（门槛触发）
  └─ PostgreSQL →（可选）Nest 外壳 → 对象存储 / 队列
```

---

## 10. 关键决策建议

1. **立刻统一验收口径**：当前阶段成功 =「模块化 + 安全/契约/状态可信」，不是「已经 Nest/PostgreSQL」。  
2. **把 composition root 与数据原语当架构 P0**，优先于继续堆新业务页。  
3. **Policy 与状态机是平台内核**，UI 与 AI 都是插件；内核不完整就扩功能，债务会指数上升。  
4. **OpenAPI 要么成为真源，要么降级为“子集契约”并写进 CI**；禁止第三种：文档暗示全量、实现偷偷更多。  
5. **换 PostgreSQL/Nest 必须设触发条件**（并发痛、合规、多实例），避免“架构升级”变成无收益大迁移。

---

## 11. 与既有文档的关系

| 文档 | 角色建议 |
|------|----------|
| `docs/architecture-refactor-plan.md` | **近端执行手册**：保行为、按域拆分、兼容导出 |
| `项目管理平台统一优化设计方案.md` | **远端北极星**：产品边界、阶段能力、目标技术栈 |
| **本文** | **架构评估与路线裁决建议**：现状评分、ROI 升级项、默认走路线 C |

建议后续若形成正式决策，可另补一份简短 ADR（Architecture Decision Record），明确：

- 当前采用路线 A / B / C 中的哪一条  
- OpenAPI 是全量真源还是子集  
- 换库/换框架的触发条件  
- `resources.ts` 与 `server.js` 残留逻辑的删除里程碑  

---

## 12. 相关路径速查

**架构与设计**

- `docs/architecture-refactor-plan.md`
- `项目管理平台统一优化设计方案.md`
- `docs/10-实现状态与差异清单.md`（偏旧 as-built，使用时注意日期）

**后端核心**

- `api/server.js`
- `api/db.js`
- `api/openapi.json`
- `api/src/modules/`
- `api/src/security/`
- `api/src/workflow/`
- `api/migrations/`

**前端核心**

- `web/src/App.tsx`
- `web/src/app/pageRegistry.tsx`
- `web/src/services/`
- `web/src/features/`
- `web/src/pages/`
- `web/src/constants/roles.ts`

**质量**

- `api/test/`
- `.github/workflows/ci.yml`

---

## 13. 结论摘要

- 架构方向 **正确**，完成度 **中上**，适合作为试点平台继续演进。  
- 优先投资：**组合根收口、数据写原语、Policy 闭环、契约真源策略、前端去巨石**。  
- 默认路线：**混合渐进（C）**——先可信与边界，后基础设施换栈。  
- 明确不做：过早微服务、未设门槛的全量换栈、自动绩效化。

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-07-13 | 初版：基于工作区架构评估与升级建议整理入库 |
