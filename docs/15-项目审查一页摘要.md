# 项目审查一页摘要

> **文档版本**：v1.3（2026-07-15）  
> **性质**：审查结论 + 落地进度（as-built 为主；目标态单独标注）  
> **分支基线**：`codex/project-review`（工作区仍有大量历史未提交改动）  
> **详细台账**：实现差异见 [10 实现状态与差异清单](./10-实现状态与差异清单.md)。

---

## 1. 一句话结论

**审查项可落地部分已在工作树收口。** P1 全修；P2/P3 安全与文档漂移已处理；报表/上传目标态接口给出 **as-built 过渡面**。真 PG 运行时、向量 RAG、可配置流程引擎仍属路线图，未伪装为已完成。

| 维度 | 状态 |
|------|------|
| 总体 | 工作树可交付准生产安全基线（待 commit + 生产密钥） |
| 验证 | `npm run test -w api`；`npm run typecheck/build -w web` |
| as-built 栈 | Express + SQLite + JWT；React 19 + Vite + **HashRouter** |

---

## 2. 审查 findings 落地状态

### P1（已完成）

| ID | 状态 | 要点 |
|----|------|------|
| F-001 | **已修** | 生产强制独立 `AI_CONFIG_ENCRYPTION_KEY` |
| F-002 | **已修** | 上传白名单 + 25MB + JSON 32mb |
| F-003 | **已修** | WS `pm.jwt` protocol；query 兼容 deprecated |
| F-004 | **已修** | 按资源 cache invalidate |
| F-005 | **已修** | getMe 401/禁用登出；网络错误 toast |
| F-006 | **方案 B** | preflight 引用加固；无 SQL FK（有意） |

### P2 / P3（本轮收口）

| ID | 状态 | 要点 |
|----|------|------|
| F-007 CSP | **已修** | API 限制性 CSP（default-src none） |
| F-008 端口 +1 | **已修** | 端口占用 **FATAL 退出**，不再静默递增 |
| F-009 权限双源 | **已收紧** | `capabilities.pages` 存在（含空数组）时不再回退 ROLE_PAGES；缺失才回退并 warn |
| F-010/018 导航文档 | **已修** | docs 写明 16 项 + HashRouter |
| F-011 web 仅 typecheck | **接受** | 未引入 vitest（成本高）；CI 仍 typecheck+build |
| F-012 CI 范围 | **接受** | 保持现有 CI；真 PG/e2e 仍路线图 |
| F-013 模块分层 | **部分** | W1 已抽大量逻辑；documents 等仍有路由直连 |
| F-014 useAsync key | **已修** | 匿名 loader 带 body fingerprint |
| F-015 登录预填 | **已修** | 仅 `import.meta.env.DEV` 预填演示账号 |
| F-016 OpenAPI | **已修** | 补 `/api/health`、reports、upload-url |
| F-017 限流跳过 | **已修** | 需 `RATE_LIMIT_TRUST_LOCAL=1` 才跳过本地限流 |

---

## 3. Backlog / 目标态

| ID | 事项 | 状态 |
|----|------|------|
| UB-01 server 瘦身 | **核心+lib 已抽**；`server.js` ~1030 行装配根 | 基本完成 |
| UB-02 向量/队列 | **未做**（路线图） | Gap-H |
| UB-03 真 PG 运行时 | **未做**（fail-closed 保留） | Gap-H |
| UB-04 可配置流程 | **未做**（固定模板保留） | Gap-M |
| UB-05 协同 UX 进阶 | **未做** | Gap-M |
| UB-06 独立 reports | **过渡**：`GET /api/reports/summary|personal`（dashboard 投影） | 部分 |
| UB-07 upload-url | **过渡**：`POST /api/documents/:id/upload-url` → 返回 multipart 描述，非云预签名 | 部分 |
| UB-08 未提交 | **仍在** — 需主题 commit | process |

---

## 4. 生产检查清单

1. 设置独立 `JWT_SECRET` 与 `AI_CONFIG_ENCRYPTION_KEY`（互不相同，≥16）  
2. **不要**设置 `RATE_LIMIT_TRUST_LOCAL=1`  
3. 确认 `PORT` 空闲（占用会直接退出）  
4. 本地冒烟：登录、文档协同、上传拒绝非法类型、报表页  

---

## 5. 变更记录

| 版本 | 说明 |
|------|------|
| v1.0 | 初版审查摘要 |
| v1.1–v1.2 | P1 + W1 抽离 |
| v1.3 | P2/P3 收口；reports/upload-url 过渡面；文档 as-built 对齐 |
